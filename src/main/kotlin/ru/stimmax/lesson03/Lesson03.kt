package ru.stimmax.lesson03

class Lesson03 {

    /*val name: String = "Alice"

    var age: Int = 30
    lateinit var phoneNumber: String

    companion object {
        const val PI_1 = 3.14
    }
    private val lazyValue: String by lazy {
        "Hello, this is a lazy String"
    }

    var score: Int = 10
    //знак ? - обозначение nullable переменной
    var speed: Double? = null

    fun example() {
        age = 31
        speed.dec()
    }

    var count: Int = 0
        get() {
            return if (field > 100) field else 0
        }
        set(value) {
            if (value >= 0) field = value
        }*/
}